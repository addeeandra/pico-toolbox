# ── LLM Provider keys (directly put this keys in config/config.json instead) ─────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-v1-...
GROQ_API_KEY=gsk_...

# ── Search keys (directly put this keys in config/config.json instead) ───────────────────────────────────────────────
BRAVE_SEARCH_API_KEY=BSA...
PERPLEXITY_API_KEY=pplx-...
SEARXNG_BASE_URL=http://searxng:8888

# ── Channel integrations (directly put this keys in config/config.json instead) ──────────────────────────────────────
TELEGRAM_BOT_TOKEN=
DISCORD_BOT_TOKEN=

# ── Container settings ────────────────────────────────────────
PICOCLAW_GATEWAY_PORT=8080
